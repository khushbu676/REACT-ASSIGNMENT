import React from 'react';

const Footer = () => {
  return (
    <footer style={styles.footer}>
      Footer
    </footer>
  );
};

const styles = {
  footer: {
    backgroundColor: '#0965c7',
    padding: '10px',
    borderRadius: '10px',
    color: '#fff',
    textAlign: 'center',
    marginTop: '10px',
  }
};

export default Footer;
